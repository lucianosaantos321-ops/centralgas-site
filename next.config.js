/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // habilita exportação estática
  distDir: 'out',   // define a pasta final como 'out'
};

module.exports = nextConfig;
